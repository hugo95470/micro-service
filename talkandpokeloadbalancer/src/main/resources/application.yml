spring:
  application:
    name: talkandpokeloadbalancer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  healthcheck:
    enabled: true
  instance:
    preferIpAddress: true
    ip-address: localhost

server:
  port: 8383